# frozen_string_literal: true

source 'https://rubygems.org'
# git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.5.3'

gem 'oj'
gem 'pry'
gem 'rest-client'
gem 'thor'

group :test do
  gem 'rspec'
  gem 'webmock'
end
